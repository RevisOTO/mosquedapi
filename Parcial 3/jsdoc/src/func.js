/**
 * Esta funcion recibe 2 numeros la base y su exponente
 * @param {Number} numero Numero base a elevar
 * @param {Number} exponente Numero de el numero de veces a elevar
 * @returns Number
 */
export function potencia(numero,exponente) {
    return numero*exponente
}